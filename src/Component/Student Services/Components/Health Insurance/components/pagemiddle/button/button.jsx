import React from 'react';
import button from './Button.module.css';

const Button = () => {
  return (
    <button className={button.button}>
      Compare and Choose Insurance
    </button>
  );
};

export default Button;
